version: 2

sources:
  - name: raw
    description: "Tables landed by dlt ingestion pipelines"
    schema: raw
    tables:
      - name: wallet_transactions
        description: "Raw rows from manual wallet/m-banking CSV exports"

models:
  - name: stg_wallet__transactions
    description: "Typed and cleaned wallet transactions"
    columns:
      - name: description_raw
        data_tests: [not_null]
      - name: direction
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["in", "out"]
